"use client";
import { useEffect,useState } from "react";
type Consent="all"|"essential";
export function CookieSettings(){const[visible,setVisible]=useState(false);useEffect(()=>{const timer=window.setTimeout(()=>setVisible(!localStorage.getItem("rh_cookie_consent")),0);const open=()=>setVisible(true);const nodes=document.querySelectorAll("[data-cookie-settings]");nodes.forEach(el=>el.addEventListener("click",open));return()=>{window.clearTimeout(timer);nodes.forEach(el=>el.removeEventListener("click",open))}},[]);const save=(value:Consent)=>{localStorage.setItem("rh_cookie_consent",value);setVisible(false)};if(!visible)return null;return <div className="cookie-layer fixed inset-x-3 bottom-3 z-[80] mx-auto max-w-3xl rounded-2xl border border-violet-200/20 bg-[#111427]/97 p-5 shadow-2xl backdrop-blur-xl" role="dialog" aria-label="Çerez tercihleri"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex-1"><p className="font-bold">Gizliliğiniz sizin seçiminiz</p><p className="mt-1 text-sm text-slate-300">Zorunlu depolama siteyi çalıştırır. Ölçüm ve reklam teknolojileri yalnız açık tercihiniz ve üretim yapılandırması sonrasında etkinleştirilir. Ayrıntılar <a className="text-violet-200 underline" href="/gizlilik">gizlilik sayfasında</a>.</p></div><div className="flex flex-wrap gap-2"><button onClick={()=>save("essential")} className="focus-ring rounded-full border border-white/20 px-4 py-2 text-sm font-bold">Yalnız zorunlu</button><button onClick={()=>save("all")} className="focus-ring rounded-full bg-violet-300 px-4 py-2 text-sm font-bold text-[#160b24]">Tümüne izin ver</button></div></div></div>}
