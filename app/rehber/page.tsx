import type { Metadata } from "next";
import { FullLink as Link } from "@/components/full-link";
import { ArrowRight, BookOpen } from "lucide-react";
import { articles } from "@/lib/articles";
export const metadata:Metadata={title:"13'lü Zodyak ve Astronomi Rehberi",description:"Burç-takımyıldız farkı, Yılancı, efemeris, presesyon, Ay fazı ve yükselen hesabını kaynaklı Türkçe rehberlerle öğrenin.",alternates:{canonical:"/rehber"}};
export default function Guides(){return <main id="ana-icerik" className="page-main"><div className="site-shell"><header className="max-w-3xl"><p className="eyebrow">Öğrenme merkezi</p><h1 className="display mt-3 text-4xl sm:text-6xl">Sonucu görmek kadar yöntemi anlamak da önemli.</h1><p className="lede mt-5">Rehberler, astronomik olgularla sembolik astroloji yorumlarını ayrı başlıklarda ele alır; kaynak ve sınırlamaları görünür tutar.</p></header><div className="mt-9 grid gap-4 md:grid-cols-2">{articles.map(article=><Link key={article.slug} href={`/rehber/${article.slug}`} className="panel group p-6 hover:border-violet-300/35"><div className="flex items-center justify-between"><span className="flex items-center gap-2 text-xs font-bold text-cyan-200"><BookOpen size={15}/>{article.category}</span><span className="text-xs text-slate-500">{article.readTime}</span></div><h2 className="mt-4 text-xl font-extrabold leading-snug">{article.title}</h2><p className="mt-3 text-sm text-slate-400">{article.description}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold text-violet-200">Rehberi oku <ArrowRight className="transition group-hover:translate-x-1" size={16}/></span></Link>)}</div></div></main>}
